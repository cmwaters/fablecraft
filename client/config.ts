import { Size } from "paper"

export const Config = {
    cardWidth: {
        min: 400,
        max: 800
    },
    
    view: {
        margin: new Size(20, 20),
        padding: new Size(20, 20)
    }

}